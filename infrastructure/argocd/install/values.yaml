crds:
  install: true
  keep: true

configs:
  cm:
    kustomize.buildOptions: --enable-helm
  params:
    server.insecure: true

server:
  ingress:
    enabled: false

notifications:
  enabled: false
