{
"id": "801b9b33",
"title": "Prepare sops-nix secret scaffolding before host identities exist",
"tags": [
"ready-for-agent"
],
"status": "closed",
"created_at": "2026-05-20T16:31:39.201Z"
}

## Parent

TODO-e9b3507c

## What to build

Prepare the sops-nix secret-management structure for the k3s cluster token while acknowledging that final host-recipient encryption waits for real Cluster Node SSH host keys.

## Acceptance criteria

- [x] The expected encrypted k3s token name and runtime path are wired into Node Configuration.
- [x] Repository configuration prevents plaintext secrets and age private keys from being committed.
- [x] Documentation explains which parts can be prepared now and which parts require unique host SSH keys from real Cluster Nodes.

## Blocked by

None - can start immediately

## Completed

- Wired `sops.secrets."k3s/token"` to `/run/secrets/k3s-token` with restrictive mode in the shared secrets module.
- Added a `.sops.yaml` recipient scaffold with admin and per-Cluster Node placeholder recipients.
- Added a placeholder `nix/secrets/k3s.yaml` documenting the expected encrypted shape without plaintext secret material.
- Tightened gitignore rules for local age keys, plaintext secret scratch files, and decrypted SOPS files while allowing encrypted `nix/secrets` files to be tracked.
- Documented pre-Proxmox secret scaffolding and post-Cluster-Node host-recipient steps in `docs/initial-setup.md`.

## Validation

- Reviewed secret wiring references for `k3s/token` and `/run/secrets/k3s-token`.
- Attempted `nix flake check --no-build --offline`; it currently fails on pre-existing host boot/filesystem assertions, not on the secret wiring.
