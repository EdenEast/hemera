{
"id": "d834da92",
"title": "Improve Node Configuration deployment scripts",
"tags": [
"ready-for-agent"
],
"status": "closed",
"created_at": "2026-05-20T16:31:39.200Z"
}

## Parent

TODO-e9b3507c

## What to build

Improve the deployment script interface so operators can apply one Cluster Node or the full Initial Setup cluster through stable commands once VMs exist.

## Acceptance criteria

- [x] Single-node deployment validates known Cluster Node names and prints clear errors.
- [x] Full-cluster deployment composes the single-node command in control-plane-then-workers order.
- [x] Host target resolution is centralized or otherwise easy to update when confirmed IPs change.

## Completed

- Added shared Cluster Node inventory in `scripts/lib/cluster-nodes.sh`.
- Updated `scripts/deploy-node` with usage, known-node validation, configurable SSH user, and dry-run support.
- Updated `scripts/deploy-cluster` to deploy the control plane before workers using shared inventory.

## Validation

- `bash -n` passes for deployment scripts.
