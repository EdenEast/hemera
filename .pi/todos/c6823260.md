{
"id": "c6823260",
"title": "Improve kubeconfig retrieval workflow",
"tags": [
"ready-for-agent"
],
"status": "closed",
"created_at": "2026-05-20T16:31:39.201Z"
}

## Parent

TODO-e9b3507c

## What to build

Improve the kubeconfig retrieval command so it reliably writes a local generated kubeconfig after the k3s control-plane Cluster Node exists, while keeping generated access material out of git.

## Acceptance criteria

- [x] The command validates prerequisites and reports clear errors when the control-plane node is unreachable.
- [x] The generated kubeconfig path remains under ignored generated output.
- [x] The command rewrites the server address to the intended control-plane LAN endpoint.

## Completed

- Updated `scripts/get-kubeconfig` to use shared Cluster Node inventory, validate SSH availability, check remote kubeconfig readability, write to ignored generated output, set restrictive permissions, and rewrite the API server address.

## Validation

- `bash -n` passes for `scripts/get-kubeconfig`.
