# Placeholder for the sops-encrypted k3s cluster token.
#
# Do not commit plaintext token material. This file should be replaced with a
# real sops-encrypted document after .sops.yaml contains the admin recipient and
# the host recipients derived from each Cluster Node's unique SSH host key.
#
# Expected decrypted shape:
#   k3s:
#     token: <strong shared cluster token>
#
# The NixOS secret name is `k3s/token`, exposed at runtime as:
#   /run/secrets/k3s-token

k3s:
  token: ENC[REPLACE_WITH_SOPS_ENCRYPTED_VALUE]
