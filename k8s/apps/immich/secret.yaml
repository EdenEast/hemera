apiVersion: v1
kind: Secret
metadata:
  name: immich-postgres
  namespace: immich
type: Opaque
stringData:
  # Replace before first apply. If this has already been applied, rotate the
  # database password deliberately instead of changing it in place.
  password: change-me-before-first-apply
